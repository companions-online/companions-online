---
name: Project State
description: Current initiatives, deadlines, active decisions — short-lived, updated often
type: project
---

No active initiatives tracked here yet.

Use this file (or sibling files in `memory/project/`) to capture ephemeral project context: who's doing what, deadlines, freezes, active incidents, recent decisions that haven't yet landed in `reference/`. These rot fast — prune aggressively.

For stable architectural/gameplay facts, write to `memory/reference/` instead.
